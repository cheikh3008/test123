{% extends 'base.html.twig' %}

{% block title %}Liste des services{% endblock %}

{% block body %}
 
  <div class="mx-auto" style="width: 600px;">
    <div class="starter-template">
      <div class="card border-secondary mb-3" style="text-align: center;" style="max-width: 100rem;">
          <div class="card-header" style="font-size: 2em;"> 
            Liste des services  <span style="float:right"><a href="{{path('add')}}"  class="btn btn-success">Ajouter</a></span></div>
            <div class="card-body">
              <table class="table table-hover">
                  <thead>
                    <tr>
                      <th scope="col">Idenfiant</th>
                      <th scope="col">Nom du service</th>
                      <th scope="col">Actions</th>
                    </tr>
                  </thead>
                  
                  <tbody>
                      {% for services in service %}
                      <tr>
                          <td>{{ services.id }}</td>
                          <td> {{ services.nomservice }} </td>
                          <td>  <a href="{{path('edit',{'id': services.id} )}}" class="btn btn-success">Modifier</a> 
                            <a href="{{path('delete',{'id': services.id} )}}"  class="btn btn-secondary"  onclick="return confirm('Etes vous sure de vouloir supprimer cet enrégistrement ?');">Supprimer </a>
                          </td>
                      </tr>
                      {% endfor %}
                  </tbody>
                  
                </table> 
              </div>
            </div>
          </div>
        </div> 
  </div>  

{% endblock %}
